# laravel-jwt-starter
Starter pack for simple REST API with JWT Authentification.

## Quickstart
```
$ git clone
$ cp .env.example .env
$ composer install
$ php artisan migrate --seed
$ php artisan serve
```
